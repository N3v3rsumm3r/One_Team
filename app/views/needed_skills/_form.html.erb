<%= form_for(@needed_skill) do |f| %>
  <% if @needed_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@needed_skill.errors.count, "error") %> prohibited this needed_skill from being saved:</h2>

      <ul>
      <% @needed_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.number_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
